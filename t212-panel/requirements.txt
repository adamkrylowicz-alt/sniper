Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-SocketIO==5.3.6
python-dotenv==1.0.1
cryptography==42.0.8
requests==2.32.3
APScheduler==3.10.4
pytz==2026.2
waitress==3.0.0

# WYŁĄCZNIE dla backtest/ibkr_data.py (dane historyczne intraday do
# backtestów, patrz plan "IBKR jako źródło danych historycznych") - reszta
# appki (T212Client, Flask) tego nie importuje, run.py działa bez tego pakietu.
# ib_async (aktywnie utrzymywany następca) wymaga Python >=3.10 - ten NAS ma
# tylko 3.8 (zweryfikowane: `python3 --version`, brak innego interpretera w
# Package Center), więc świadomie ib_insync (archiwalny, ale wspiera >=3.6,
# API historycznych danych jest stabilne od lat - niskie ryzyko dla wąskiego,
# nie-transakcyjnego użycia tutaj).
ib_insync==0.9.86
